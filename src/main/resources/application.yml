server:
  port: 8081
  servlet:
    context-path: /
spring:
  datasource:
    driver-class-name: org.h2.Driver
    password: admin
    url: jdbc:h2:mem:financescontrolldb
    username: admin
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
